# External Tooling Note

ModelX v1 is a self-contained guided planner. The active backend flow does not call external tool providers.

The ModelX agents use the user's project idea, goal, skill level, data status, and constraints to produce a structured ML Blueprint.

Future versions can add dataset discovery or learning-resource search as a separate capability, but those integrations are intentionally out of scope for the current planner.
